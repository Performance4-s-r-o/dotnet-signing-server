// Intentionally empty. Custom fillable-field validation (required + type/format) now
// lives entirely on the v0 server (lib/sharepoint/external-sign/fields.ts), enforced when
// the signer fills the PDF. The signing engine treats field values as opaque text.
// This file can be deleted from source control.
